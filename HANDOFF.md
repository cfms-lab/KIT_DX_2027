# 미래융합대학 교육과정 회의자료 작업 핸드오프

작성일: 2026-08-09  
작업 루트: `D:\OneDrive\Documents\__잡무(backup_20190531)\_2026\_미래융합대학_교육과정`

## 1. 가장 먼저 확인할 사항

사용자의 마지막 요청은 아직 적용하지 않았다. 다음 작업의 최우선 목표는 아래와 같다.

> 현재 1페이지 그림을 A4 가로에서 **A4 세로**로 변경한다.  
> **AX단과대학, 미래융합대학(DX대학), 자율전공학부 유형 I, 기타 모집단위**의 네 단위를 같은 높이·같은 선상에 배치한다.

현재 `output\DX대학_자율전공_1학년_교육과정_회의브리프_1p.docx`는 그 직전 단계까지 완성된 **A4 가로 버전**이다. 최신 요청 직후 작업이 중단되었으므로 세로 전환은 시작하지 않았다.

## 2. 현재 완성본의 상태

### 1페이지 흐름도

- 파일: `output\DX대학_자율전공_1학년_교육과정_회의브리프_1p.docx`
- 생성 스크립트: `analysis\build_dx_enrollment_flow_brief.py`
- 삽입 이미지: `output\assets\dx_enrollment_flow_mindmap.png`
- 현재 페이지: A4 가로 1페이지
- 현재 그림의 주요 특징:
  - AX단과대학 박스는 하나만 존재한다.
  - 미래융합대학(DX대학) 박스도 하나만 존재한다.
  - 각 단과대학 박스 안에서 `자율전공 유형 II`가 위에 먼저 나오고, 아래에 소속 학과·전공 영역이 있다.
  - 유형 II는 1학년 과정만 운영되고, 1학년 말 곡선 화살표를 따라 해당 단과대학 내부 전공으로 내려간다.
  - 유형 I은 별도 박스로 표시되고, 1학년 말 AX·미래융합대학·기타로 이동하는 점선 곡선 화살표가 있다.
  - 직접입학·유형 I·유형 II·기타의 입학선 두께가 서로 다르다.
  - 정원 숫자는 그림에 직접 쓰지 않는다.

### 2페이지 교과목 교집합 표

- 파일: `output\DX대학_자율전공_1학년_교육과정_회의브리프_2p.docx`
- 생성 스크립트: `analysis\build_dx_course_matrix_only.py`
- 이 파일은 현재 흐름도 수정 작업과 별개이며, 사용자가 별도로 요청하지 않는 한 수정하지 않는다.

## 3. 다음 세로 버전의 권장 레이아웃

A4 세로에서는 다음 구조가 가장 자연스럽다.

1. 페이지 상단 중앙에 `전체 신입생 입학` 루트 노드를 둔다.
2. 그 아래 동일한 Y 좌표에 네 개의 큰 단위 박스를 한 줄로 배치한다.
   - AX단과대학
   - 미래융합대학(DX대학)
   - 자율전공학부 유형 I
   - 기타 모집단위
3. 네 박스의 상단·높이·기본 폭을 가능한 한 통일한다.
4. AX와 미래융합대학 박스 내부는 다음 순서를 지킨다.
   - 가장 위: `자율전공 유형 II` / `1학년 과정만 운영`
   - 중간 또는 옆 진입선: `소속 학부·학과(모집단위) 직접입학`
   - 아래: 해당 단과대학 소속 학과·전공
5. 미래융합대학 박스만 아래쪽을 더 확장해 9개 전공명을 보여준다.
6. 유형 I 박스에서 1학년 말 AX·미래융합대학·기타로 이동하는 곡선 점선 화살표를 그린다.
7. 유형 II 박스 내부에서도 1학년 과정에서 해당 단과대학 전공 영역으로 내려가는 곡선 화살표를 사용한다.
8. AX·미래융합대학을 입학 박스와 도착 박스로 중복해 만들지 않는다. 각 단과대학은 반드시 하나의 통합 박스여야 한다.

세로 캔버스는 약 `2400 x 3300 px`로 만들고 Word에 약 `7.1~7.2 in` 폭으로 삽입하면 A4 세로에서 충분한 해상도를 유지할 수 있다.

## 4. 흐름과 선 두께에 관한 사용자 결정

사용자가 확정한 개념은 다음과 같다.

- 자율전공 유형 I과 유형 II는 모두 **1학년 과정만** 존재한다.
- 유형 I 학생은 1학년 말 대학 전체 학과·전공 중에서 선택한다.
- 유형 II 학생은 입학한 단과대학 내부 학과·전공 중에서 선택한다.
- 유형 I·II 학생의 1학년 말 이동은 직선보다 **곡선 화살표**로 표현한다.
- 입학정원 숫자를 노드에 직접 쓸 필요는 없다.
- 대신 입학 단계 edge의 두께는 정원 규모의 상대비를 반영한다.
- 자율전공 유형 I:II의 상대 규모는 **약 20:10**으로 가정한다.
- 1학년 말 실제 전공 선택 결과는 고정 정원이 아니므로, 유형 I의 목적지별 점선 화살표는 동일한 가는 두께로 그려도 된다.

현재 스크립트의 시각적 가중치는 다음과 같다.

```python
w_ax_direct = 38
w_dx_direct = 25
w_other = 11
w_type1 = 19
w_ax_type2 = 6
w_dx_type2 = 4
```

이는 숫자를 표시하기 위한 값이 아니라 선 두께만 정하기 위한 작업값이다. 유형 I:II 전체 규모는 대략 20:10 관계가 되도록 잡았다. AX/DX 유형 II의 개별 배분은 공식 정원표가 없어 도식용 상대치다.

## 5. 미래융합대학 소속 전공

서류상 단과대학 공식 명칭은 **미래융합대학**이다. `DX대학`은 병기 또는 설명용 명칭으로만 사용한다.

미래융합대학에 표시해야 할 전공은 다음 9개다.

- 건축공학전공
- 토목공학전공
- 산업공학전공
- 수리빅데이터전공
- 고분자공학전공
- 신소재공학전공
- 소재디자인공학전공
- 화학공학전공
- 화학생명소재전공

기존 학부 묶음으로 정리할 경우:

- 건축토목환경공학부: 건축공학전공, 토목공학전공
- 산업·빅데이터공학부: 산업공학전공, 수리빅데이터전공
- 재료공학부: 고분자공학전공, 신소재공학전공
- 화학소재공학부: 소재디자인공학전공, 화학공학전공, 화학생명소재전공

## 6. 규정에서 확인한 핵심 내용

원문:

- `src\국립금오공과대학교_학사운영_규정_전문202606.docx`
- `src\국립금오공과대학교_학사운영_규정_전문202606.hwpx`
- `src\국립금오공과대학교_학사운영_규정_전문202606.pdf`
- 텍스트 코퍼스: `graph_corpus\regulations\학사운영_규정_202606.md`

관련 조항:

- 제13조 제1항: 자율전공학부 학생의 선택으로 추가되는 학과·전공별 인원은 별도 정원으로 관리하며 별표 1에 포함하지 않는다.
- 제13조 제2항: 전공별 배정인원은 학부에서 배정정원의 70~130% 범위로 조정할 수 있다.
- 제14조: 학과·전공 배정은 원칙적으로 1학년 말이다. 학부별로 별도 시기를 정할 수 있다.
- 제16조 제3·4항: 학생 지망 순이 원칙이며, 지망자가 배정정원을 초과하면 학부가 별도 기준을 정한다.
- 제16조 제6항: 자율전공학부의 전공배정 세부사항은 별도로 정한다.

규정 별표 1에서 확인한 정원 숫자는 이전 검증용으로만 사용했다. 현재 사용자는 숫자를 그림에 표기하지 않기로 했다.

- 전체 입학정원 합계: 1,239명
- 기존 모집단위: 904명
- 자율전공학부: 335명
- 미래융합대학 9개 전공의 현행 배정정원 합계: 305명

이 숫자는 선 두께의 상대비를 판단할 때만 참고하고, 최종 그림에는 직접 쓰지 않는다.

## 7. 문서 생성 및 렌더링 방법

### DOCX 재생성

번들 Python:

```powershell
& 'C:\Users\USER\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' `
  'analysis\build_dx_enrollment_flow_brief.py'
```

스크립트는 프로젝트 루트를 상대경로로 계산하므로 원격 PC에서도 프로젝트 폴더가 다른 드라이브에 있더라도 대체로 그대로 실행된다. 다만 다음 Windows 글꼴이 필요하다.

- `C:\Windows\Fonts\malgun.ttf`
- `C:\Windows\Fonts\malgunbd.ttf`

### 렌더링 검증

문서 플러그인의 `render_docx.py`는 이 PC에서 반복적으로 멈췄다. LibreOffice와 Poppler를 직접 호출하는 방식이 안정적이었다.

```powershell
$qa = 'C:\temp\dx_flow_qa'
$profile = 'C:\temp\lo_profile_dx_flow_unique'
New-Item -ItemType Directory -Force -Path $qa,$profile | Out-Null
$profileUri = 'file:///' + ($profile -replace '\\','/')

& 'C:\Program Files\LibreOffice\program\soffice.com' --headless `
  "-env:UserInstallation=$profileUri" `
  --convert-to pdf --outdir $qa `
  'output\DX대학_자율전공_1학년_교육과정_회의브리프_1p.docx'

$pdf = (Get-ChildItem -LiteralPath $qa -Filter '*.pdf' | Select-Object -First 1).FullName
& 'C:\texlive\2026\bin\windows\pdftoppm.exe' -png -r 170 -singlefile $pdf "$qa\page-1"
& 'C:\texlive\2026\bin\windows\pdfinfo.exe' $pdf
```

검증 기준:

- A4 세로 1페이지인지 확인
- 네 단위 박스가 동일한 Y 좌표에 있는지 확인
- AX·미래융합대학 박스가 각각 하나뿐인지 확인
- 굵은 입학선이 다른 박스 뒤를 통과해 출발지가 잘못 보이지 않는지 확인
- 유형 I 점선 곡선이 다른 박스의 텍스트를 가리지 않는지 확인
- 유형 II가 직접입학보다 먼저 보이고, 내부 곡선 화살표가 학과·전공 영역으로 내려가는지 확인
- 미래융합대학 9개 전공명이 세로 A4에서도 읽히는지 확인

현재 마지막 가로 버전의 QA 이미지는 아래에 있다.

`C:\Users\USER\.codex\visualizations\2026\08\09\019fe5e6-fbe8-7a72-b63b-9b36229c5529\weighted_flow_qa2\page-1.png`

## 8. 데이터·Graphify 구축 상태

다른 학과가 재사용할 수 있도록 이미 다음 자산이 구축되어 있다.

- `data\departments.csv`
- `data\course_offerings.csv`
- `data\course_overlap.csv`
- `data\quality_issues.csv`
- `data\dx_curriculum.sqlite`
- `data\README.md`
- `graph_corpus\overview.md`
- `graph_corpus\departments\*.md`
- `graph_corpus\regulations\학사운영_규정_202606.md`
- `graphify-out\graph.json`
- `graphify-out\graph.html`
- `graphify-out\GRAPH_REPORT.md`

Graphify 기존 그래프 질의 예:

```powershell
& 'C:\Users\USER\.local\bin\graphify.exe' query `
  '자율전공학부 전공 선택 및 배정 학과 전공 배정' --budget 2000
```

`graphify-out\.graphify_python`에는 현재 Graphify 환경의 Python 경로가 기록되어 있다. 원격 PC에서 경로가 달라지면 다시 생성해야 한다.

## 9. 작업 시 주의사항

- 기존 `output\..._2p.docx`는 건드리지 않는다.
- 최종 1페이지 파일명은 바꾸지 않고 같은 경로에 덮어쓴다.
- 공식 명칭은 `미래융합대학`, 설명용 병기는 `미래융합대학(DX대학)`으로 한다.
- 정원 숫자를 노드에 다시 넣지 않는다.
- 유형 I:II의 20:10은 공식 확정값이 아니라 사용자 요청에 따른 근사 가정이다.
- Type I 학생의 학과별 실제 이동량은 미정이므로 목적지 곡선의 두께로 확정 비율을 암시하지 않는다.
- 페이지 방향만 바꾸고 내용을 단순 축소하면 글자가 너무 작아진다. 반드시 세로용 레이아웃을 다시 설계한다.
- 현재 폴더에서 `git status`가 과거에 `not a git repository`를 반환한 적이 있으므로, GitHub 게시 전에 실제 저장소 초기화 여부를 다시 확인한다.

## 10. 완료 조건

다음 네 조건이 모두 만족되면 내일 작업이 완료된 것이다.

1. A4 세로 1페이지다.
2. AX·미래융합대학·자율전공 유형 I·기타가 같은 선상에 있다.
3. AX와 미래융합대학은 각각 하나의 통합 박스로만 보인다.
4. 유형 I·II의 1학년 말 전공 선택 이동이 겹치지 않는 곡선 화살표로 명확히 보인다.

